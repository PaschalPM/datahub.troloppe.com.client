import { KeyupEscapeDirective } from './keyup-escape.directive';

describe('KeyupEscapeDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyupEscapeDirective();
    expect(directive).toBeTruthy();
  });
});
