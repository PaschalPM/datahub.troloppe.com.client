import { ClickSelfDirective } from './click-self.directive';

describe('ClickSelfDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickSelfDirective();
    expect(directive).toBeTruthy();
  });
});
