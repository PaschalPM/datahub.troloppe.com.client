import { DropdownFunctionality } from './dropdown-functionality';

describe('DropdownFunctionality', () => {
  it('should create an instance', () => {
    expect(new DropdownFunctionality()).toBeTruthy();
  });
});
