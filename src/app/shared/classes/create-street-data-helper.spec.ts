import { CreateStreetDataHelper } from './create-street-data-helper';

describe('CreateStreetDataHelper', () => {
  it('should create an instance', () => {
    expect(new CreateStreetDataHelper()).toBeTruthy();
  });
});
