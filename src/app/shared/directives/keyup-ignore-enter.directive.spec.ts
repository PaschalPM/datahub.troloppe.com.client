import { KeyupIgnoreEnterDirective } from './keyup-ignore-enter.directive';

describe('KeyupIgnoreEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyupIgnoreEnterDirective();
    expect(directive).toBeTruthy();
  });
});
