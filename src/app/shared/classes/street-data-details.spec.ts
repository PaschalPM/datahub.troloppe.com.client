import { StreetDataDetails } from './street-data-details';

describe('StreetDataDetails', () => {
  it('should create an instance', () => {
    expect(new StreetDataDetails()).toBeTruthy();
  });
});
